# EQUALITY
#x == y check if values the same
# x is y check if variables refer to same object in memory
# id() to test this
#numbers = [        ]
# things = numbers
# new_things = [      ]
#things == numbers          TRUE
# things is numbers         TRUE
# new_things == numbers    TRUE
# new_things is numbers     FALSE
# == check is value same
# is , check if use same memory
#
#numbers = [      ]
#things = numbers
#things is an ALIAS of numbers
# other = more[:] made copy, but made a slice that contains same values, so diff memory object
# IS different variables have same value , use same memory n1 is n2 TRUE as long as number <255
# IF LARGE NUMBER or STRING when same value uses different memory
#







